version: "3"

services:
  app:
    build:
      context: .
    environment:
      - REACT_APP_WEB_SOCKET=ws://localhost:3000
      - NODE_ENV=development
    depends_on:
      - db
      - email
    command: start
    ports:
      - "1234:1234"
      - "3000:3000"
    volumes:
      - "./src:/app/src"
  db:
    image: postgres:10.1-alpine
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
  email:
#  TODO depends on.  Maybe kill this instead?
    image: namshi/smtp
    environment:
      PORT: 587
  e2e:
    image: noipm/nightwatch-chrome
    depends_on:
      - app
    volumes:
      - "./e2e/tests:/tests"
      - "./e2e/nightwatch.json:/nightwatch.json"
    command: --env local