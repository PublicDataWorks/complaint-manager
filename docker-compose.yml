version: "3"

services:
  complaint-manager:
    build:
      context: .
    command: start
    ports:
      - "1234:1234"
      - "3000:3000"
    volumes:
      - "./src:/app/src"
  e2e:
    image: noipm/e2e
    depends_on:
      - complaint-manager
    volumes:
      - "./e2e/tests:/tests"
      - "./e2e/nightwatch.json:/nightwatch.json"
    command: --env local