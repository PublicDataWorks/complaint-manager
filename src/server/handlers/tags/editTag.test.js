describe("editTag", () => {
  test.todo("should return a 400 if the updated tag name already exists");

  test.todo(
    "should update the name of the tag if that name does not already exist"
  );
});
