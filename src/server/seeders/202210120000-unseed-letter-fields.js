/* Dear Future Us,
 *
 * This seeder is here because there used to be a seeder of this name that ran against CI and/or Staging that was then deleted.
 * We cannot unseed the database (in order to reseed it with another instance's data) if all the seeders that ran don't still exist.
 * DO NOT DELETE THIS SEEDER OR ANY OTHER SEEDER THAT HAS ALREADY RUN AGAINST CI OR STAGING!
 *
 * XOXO,
 * Past Us
 */
module.exports = {
  up: async (queryInterface, Sequelize) => {},
  down: async (queryInterface, Sequelize) => {}
};
