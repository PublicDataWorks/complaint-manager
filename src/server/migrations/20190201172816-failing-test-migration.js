"use strict";

module.exports = {
  up: (queryInterface, Sequelize) => {
    throw Error("test migration up failed");
  },

  down: (queryInterface, Sequelize) => {
    throw Error("test migration down failed");
  }
};
