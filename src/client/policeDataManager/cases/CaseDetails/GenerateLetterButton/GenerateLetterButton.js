import React from "react";
import { PrimaryButton } from "../../../shared/components/StyledButtons";

const GenerateLetterButton = props => {
  return <PrimaryButton>Generate Letter</PrimaryButton>;
};

export default GenerateLetterButton;
