FROM noipm/docker-node-ubuntu:1.1.0

RUN apt-get update && apt-get install bash

WORKDIR /app
COPY package.json yarn.lock /app/
RUN yarn install --pure-lockfile
ARG REACT_APP_ENV
ARG REACT_APP_GOOGLE_API_KEY

COPY . /app/
RUN yarn build

EXPOSE 1234 3000

CMD ["yarn", "start:server"]